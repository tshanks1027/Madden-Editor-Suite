/**
 * Renderer process entry point
 * Loads the Vanilla JS Madden Editor Suite application
 */

// Import the main Vanilla JS app
import './renderer/js/app.js';

console.log('👋 Madden Editor Suite renderer process loaded');